// API configuration
// Update this to point to your running backend.
// Default backend dev port comes from beta backend .env.example -> PORT=3000
// Note: On Android emulator, use http://10.0.2.2:3000 instead of localhost.
// For iOS simulator, http://localhost:3000 works.
// For Flutter Web dev, ensure CORS allows your origin (see backend CORS_ALLOWED_ORIGINS).

const String apiBaseUrl = 'https://eventos-app-y5kf.onrender.com/api';
// Public base (for images and static files under /uploads or relative paths)
const String apiPublicBase = 'https://eventos-app-y5kf.onrender.com';
